# Rules (Windows) — SCAFFOLDED

Phase 3 produces the validated rule files. Scaffold copies from Mac and adds Windows-specific entries to `paths.deny` (AppData credential paths, registry export patterns) and `commands.deny` (PowerShell EncodedCommand, registry-write commands).

Reference: `mac/harness/rules/README.md`.
