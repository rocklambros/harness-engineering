# Rules (Jetson) — SCAFFOLDED

Phase 3 of the Jetson build produces the validated rule files. The scaffold is a copy from Mac plus Jetson-specific path/command additions surfaced in Phase 2.

See `mac/harness/rules/README.md` for structure. Jetson-specific extensions land in `paths.deny` (Tegra paths) and `commands.deny` (power-management commands).
